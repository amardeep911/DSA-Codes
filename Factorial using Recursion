#include<iostream>

using namespace std;

int sum(int n){
    
   if (n == 0)
   return 1;
   else 
   return sum(n-1)*n;
   
}
int main()
{   
    int n,total;
    
    total = sum(5);
    printf("%d", total);
    
    
    return 0;
}
